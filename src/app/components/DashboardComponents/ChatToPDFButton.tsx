import { Button } from "@/components/ui/button";
import React from "react";

const ChatToPDFButton = () => {
  return (
    <Button
      variant="default"
      className="text-sm sm:text-md text-wrap py-2 px-1"
    >
      Chat
    </Button>
  );
};

export default ChatToPDFButton;
